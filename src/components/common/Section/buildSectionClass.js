// якщо десь падінг топ відрізняється від дефолтного, можна задати додаткові класи
export const buildSectionClassName = (type) => {
  switch (type) {
    case "":
      return;
    default:
      return "";
  }
};
